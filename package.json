{
  "name": "deepseek-balance",
  "displayName": "deepseek-balance",
  "description": "Show DeepSeek balance in status bar",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.118.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "deepseek-balance.refresh",
        "title": "DeepSeek Balance: Refresh"
      },
      {
        "command": "deepseek-balance.setApiKey",
        "title": "DeepSeek Balance: Set API Key"
      }
    ]
  },
  "configuration": {
    "properties": {
      "deepseekBalance.refreshIntervalMinutes": {
        "type": "number",
        "default": 10
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package:vsix": "npx @vscode/vsce package --allow-missing-repository --skip-license",
    "publish:vsce": "npx @vscode/vsce publish --allow-missing-repository --skip-license",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "publisher": "liooil",
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.19.17",
    "@types/vscode": "^1.118.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "@vscode/vsce": "^3.9.1",
    "eslint": "^9.39.3",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1"
  }
}
